<configuration scan="true" scanPeriod="30 seconds">

  <property name="LOG_PATH" value="logs/" />
  <property name="LOGGING_PATTERN" value="%-5level %d{yyyy-MM-dd HH:mm:ss} [%thread] %-32.32logger{0} %C %M %msg%n" />

  <!-- 파일 설정 -->
  <!--<appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <rollingPolicy
      class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
      &lt;!&ndash; 파일 이름 패턴 (날짜와 순번 포함) &ndash;&gt;
      <fileNamePattern>${LOG_PATH}/app.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
      &lt;!&ndash; 파일 최대 크기 &ndash;&gt;
      <maxFileSize>10MB</maxFileSize>
      &lt;!&ndash; 최대 보관 파일 개수 &ndash;&gt;
      <maxHistory>30</maxHistory>
      &lt;!&ndash; 로그 파일이 보관될 디렉토리 크기 제한 &ndash;&gt;
      <totalSizeCap>1GB</totalSizeCap>
    </rollingPolicy>

    <encoder>
      <charset>UTF-8</charset>
      <pattern>${LOGGING_PATTERN}</pattern>
    </encoder>
  </appender>-->

  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <charset>UTF-8</charset>
      <pattern>${LOGGING_PATTERN}</pattern>
    </encoder>
  </appender>

  <root level="INFO">
<!--    <appender-ref ref="FILE" />-->
    <appender-ref ref="CONSOLE" />
  </root>
</configuration>
