subprojects {
	group = 'com.bp'
	version = '0.0.1-SNAPSHOT'

	apply plugin: 'java'

	sourceCompatibility = '17'

	repositories {
		mavenCentral()
		maven { url "https://packages.confluent.io/maven/"}
	}
}
